# 1. Create and print an array filled with zeros called zero_array, 
# which has two rows and four columns.
import numpy as np

zero_array = np.zeros((2,4))
print(zero_array)


# 2. Create and print an array of random floats between 0 and 1 called random_array, 
# which has three rows and six columns.


random_array = np.random.rand(3,6)
print(random_array)